name: nichefate-dev
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pytest
  - ruff
  - black
  - ipykernel
  - jupyter
  - nbconvert
  - pyyaml
  - pandas
  - matplotlib
  - seaborn
  - tabulate
  - pip
  # Draft development environment only. This file is not locked.
