# Benchmark Limitations

- L126 serial sections are not timepoints.
- The benchmark supports spatial clone and niche characterization only.
- It does not support future-dynamics claims on its own.
- Reference-only clone calling is a conservative benchmark, not the primary
  layer.
- QC warnings remain part of the frozen result set.
